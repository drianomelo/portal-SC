<a href="{{ $href }}" class="flex items-center gap-6 px-8 py-4 text-sm font-extrabold uppercase border border-gray-300 rounded-full text">
    {{ $name }}
    <i class="fa-solid fa-{{$icon}}"></i>
</a>
